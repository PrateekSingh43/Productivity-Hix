# Express API Local Configuration

API_PORT="4000"
WEB_ORIGIN="http://localhost:3000"

# Essential for bypassing OAuth during local development testing
ALLOW_DEV_AUTH="true"

# Local database URL. Put real credentials only in apps/api/.env or packages/db/.env.
DATABASE_URL="postgresql://postgres:password@localhost:5432/productivehix"
